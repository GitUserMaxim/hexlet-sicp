<p>Using the results of exercises <a href="{{ route('exercises.show', App\Models\Exercise::findByPath('1.16')) }}">1.16</a>
 and <a href="{{ route('exercises.show', App\Models\Exercise::findByPath('1.17')) }}">1.17</a>
, devise a procedure 
<code>mul-iter</code>
 that generates an iterative process for multiplying two integers in terms of adding, doubling, and halving and uses a logarithmic number of steps.</p>

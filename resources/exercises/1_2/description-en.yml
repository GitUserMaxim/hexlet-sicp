name: 'Into prefix form'
description: |
  <p>Translate the following expression into prefix form:</p>
  <pre><code>5 + 4 + (2 − (3 − (6 + 4/5)))
  -----------------------------
        3(6 - 2)(2 - 7)</code></pre>

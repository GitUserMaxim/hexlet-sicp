<p>Программа Лизы неполна, поскольку она не определила, как реализуется абстракция интервала. Вот определение конструктора интервала:</p>
<pre><code>(define (make-interval a b) (cons a b))
</code></pre>
<p>Завершите реализацию, определив селекторы 
<code>upper-bound</code>
 и 
<code>lower-bound</code>
.
</p>

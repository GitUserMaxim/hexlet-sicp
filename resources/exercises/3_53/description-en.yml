name: 'Description of the elements of the stream'
description: |
  <p>Without running the program, describe the elements of the stream defined by</p>
  <pre><code>(define s (cons-stream 1 (add-streams s s)))
  </code></pre>

<p>Modify the pairs procedure so that 
<code>(pairs integers integers)</code>
 will produce the stream of all pairs of integers 
<code>(i, j)</code>
 (without the condition 
<code>i ≤ j</code>
). Hint: You will need to mix in an additional stream.
</p>

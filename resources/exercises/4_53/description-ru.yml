name: 'Результат вычисления'
description: |
  <p>Если у нас есть 
  <code>permanent-set!</code>
  , описанное в упражнении 
  <a href="{{ route('exercises.show', App\Models\Exercise::findByPath('4.51')) }}">4.51</a>
  , и 
  <code>if-fail</code>
   из упражнения 
  <a href="{{ route('exercises.show', App\Models\Exercise::findByPath('4.52')) }}">4.52</a>
  , то каков будет результат вычисления</p>
  <pre><code>(let ((pairs '()))
    (if-fail (let ((p (prime-sum-pair '(1 3 5 8) '(20 35 110))))
               (permanent-set! pairs (cons p pairs))
               (amb))
             pairs))
  </code></pre>

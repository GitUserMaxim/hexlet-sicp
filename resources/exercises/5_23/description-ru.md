<p>Расширьте вычислитель так, чтобы обрабатывались производные выражения 
<code>cond</code>
, 
<code>let</code>
 и тому подобные (раздел 4.1.2). Можно «сжульничать» и считать, что синтаксические трансформации вроде 
<code>cond->if</code>
 имеются как машинные операции.
</p>
